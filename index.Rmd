---
title: "Física Ambiental"
author: "Jean José Lima de Bastos"
date: "2023-03-16"
output: html_document
runtime: shiny
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

1.  (ENEM) Muitas usinas hidroelétricas estão situadas em barragens. As características de algumas das grandes represas e usinas brasileiras estão apresentadas no quadro abaixo.

![](images/QUESTION1.png)

A razão entre a área da região alagada por uma represa e a potência produzida pela usina nela instalada é uma das formas de estimar a relação entre o dano e o benefício trazidos por um projeto hidroelétrico. A partir dos dados apresentados no quadro, o projeto que mais onerou o ambiente em termos de área alagada por potência foi:

```{r echo=FALSE}

library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)Tucuruí", "B)Furnas", "C)Itaipu", "D)Ilha Solteira", "E)Sobradinho")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {

  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "E)Sobradinho") {
      "Parabéns! Você acertou a pergunta. Essa questão trabalha com: usinas hidrelétricas / barragens / represas"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)

```

2.  (ENEM) O esquema abaixo mostra, em termos de potência (energia/tempo), aproximadamente, o fluxo de energia, a partir de uma certa quantidade de combustível vinda do tanque de gasolina, em um carro viajando com velocidade constante.

![](images/QUESTION2.png)

O esquema mostra que, na queima da gasolina, no motor de combustão, uma parte considerável de sua energia é dissipada. Essa perda é da ordem de:

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)80%", "B)70%", "C)50%", "D)30%", "E)20%")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {

  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "A)80%") {
      "Parabéns! Você acertou a pergunta. Essa questão trabalha com: usinas hidrelétricas / barragens / represas"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)

```

3.  (UFSM-RS) Na superfície da Terra, um certo corpo flutua dentro de um recipiente com um líquido incompressível. Se esse sistema for levado à Lua, onde a aceleração gravitacional é menor, o corpo:

    ![](images/QUESTION4.png)

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c(
  "A) Submerge, atingindo o fundo do recipiente",
  "B) Flutua, porém com uma porção maior submersa", 
  "C) Flutua com a mesma porção submersa",
  "D) Flutua, porém com uma porção menor submersa", 
  "E) Submerge completamente, mas sem atingir o fundo do recipiente")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "A) Submerge, atingindo o fundo do recipiente") {
      "Parabéns! Você acertou a pergunta. Essa questão trabalha com: Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

04.(UNIFOR-CE) O esquema abaixo representa as três fases de uma substância pura, e as setas indicam algumas mudanças de fases possíveis.

As setas x, y e z correspondem, respectivamente, a:

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	Liquefação, vaporização e condensação", "B) Fusão, vaporização e sublimação", "C) Liquefação, condensação e vaporização", "D) Fusão, sublimação e vaporização", "E) Solidificação, liquefação e sublimação")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "B) Fusão, vaporização e sublimação") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

5.  (UFRN) Um copo de água está à temperatura ambiente de 30 °C. Joana coloca cubos de gelo dentro da água. A análise dessa situação permite afirmar que a temperatura da água irá diminuir porque:

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	O gelo irá transferir frio para a água", "B)	A água irá transferir calor para o gelo", "C)	O gelo irá transferir frio para o meio ambiente", "D)	A água irá transferir calor para o meio ambiente", "E) N.D.A")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "B)	a água irá transferir calor para o gelo") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

6.  (FEI-SP) Considerando as faixas audíveis para os animais mencionados a seguir, podemos afirmar que:

                gato – 30 Hz até 45 kHz
                cão – 20 Hz até 30 kHz
               homem – 20 Hz até 20 kHz
               baleia – 40 Hz até 80 kHz

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	o homem pode escutar sons mais graves que o gato", "B)	a baleia pode escutar sons mais graves que o cão", "C)	o cão escuta sons mais agudos que a baleia", "D)	o homem escuta sons mais agudos que a baleia", "E) o gato escuta sons mais graves que o cão")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "A)	o homem pode escutar sons mais graves que o gato") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

7.  (UNICRUZ-RS) Num dia chuvoso, uma pessoa vê um relâmpago entre uma nuvem e a superfície da Terra. Passados 6 s ela ouve o som do trovão correspondente. Sabendo que a velocidade do som no ar é 340 m/s, qual a distância entre a pessoa e o ponto onde ocorreu o relâmpago?

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	2 040 m", "B)	56,6 m", "C)	1 020 m", "D)	2 400 m", "E)	Não é possível calcular essa distância.")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "A)	2 040 m") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

8.  **(CEFET-RJ)** Vários estudos têm concluído que, em virtude do efeito estufa, do comprometimento da camada de ozônio e de outros fatores, há grande possibilidade de fusão das camadas de gelo das calotas polares e, em consequência, o nível das águas dos oceanos se elevará. Supondo-se que houvesse a fusão da massa total de gelo das calotas polares (m = 4,0 x 10^8^ toneladas, a uma temperatura média de -10 °C), a quantidade de calor necessária para que a massa total se liquefizesse seria igual a: Dados: C~gelo~ = 0,5 cal/g°C e L = 80 cal/g.

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	32 x 109 cal", "B)	34 x 109 cal", "C)	2 x 1011 cal", "D)	32 x 1015 cal", "E)	34 x 1015 cal")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "E)	34 x 1015 cal") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

9.  **(VUNESP-SP / Adaptada)** A fotossíntese é uma reação bioquímica que ocorre nas plantas, para a qual é necessária a energia da luz do Sol, cujo espectro de frequência é dado a seguir. Sabendo que a fotossíntese ocorre predominantemente nas folhas verdes, de qual ou quais faixas de frequências do espectro da luz solar as plantas absorvem menos energia nesse processo?

![](images/QUESTION9.png)

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	5,2 x 1014 a 6,1 x 1014", "B)	7,7 x 1011 a 9,2 x 1011", "C)	3,2 x 1010 a 4,1 x 1010", "D)	9,9 x 1014 a 10,2 x 1014", "E)	8,8 x 1023 a 12,6 x 1023 ")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "A)	5,2 x 1014 a 6,1 x 1014") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

10. **(UFMT / Adaptada)** Um estudante deseja saber quantas árvores por minuto uma usina termelétrica precisa para abastecer com energia elétrica uma cidade do tamanho de Cuiabá. Para fazer uma estimativa desse número, considerou que:

I.    a cidade de Cuiabá consome 10 kWh por segundo de energia elétrica

II.  um quilo de madeira é capaz de prover energia suficiente para elevar a temperatura de 5 litros de água de 30ºC para 100ºC

III.uma árvore utilizada numa usina termelétrica corresponde a uma tonelada de madeira

IV\.  o processo de conversão de energia térmica para elétrica numa usina termelétrica tem um fator de eficiência de 50%. Dado que o calor específico da água é 4 J/gºC, qual o número inteiro que mais se aproxima do número de árvores por minuto que o estudante encontrou em sua estimativa?

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	1 árvore", "B) 2 árvores", "C) 3 árvores", "D) 4  árvores", "E)	5  árvores.")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "C)	3  árvores.") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

11. **(ENEM)** A tabela a seguir apresenta alguns exemplos de processos, fenômenos ou objetos em que ocorrem transformações de energia. Nessa tabela, aparecem as direções de transformações de energia. Por exemplo, o termopar é um dispositivo onde energia térmica se transforma em energia elétrica.

![](images/QUESTION11-01.png)

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	em todos os processos", "B)	somente nos processos que envolvem transformações de energia sem dissipação de calor", "C)	somente nos processos que envolvem transformações de energia mecânica", "D)	somente nos processos que não envolvem energia química", "E)	somente nos processos que não envolvem nem energia química nem energia térmica.")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "A)	em todos os processos") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

12. **(UFU-MG)** Normalmente, as distâncias entre os fios (desencapados) da rede elétrica de alta-tensão são inferiores às distâncias entre as pontas das asas de algumas aves quando em vôo. Argumentando que isso pode causar a morte de algumas aves, ecologistas da região do Pantanal Mato-grossense têm criticado a empresa de energia elétrica da região. Em relação a esta argumentação, pode-se afirmar que:

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	Os ecologistas não têm razão, pois sabe-se que é nula a resistência elétrica do corpo de uma ave.", "B)	Os ecologistas têm razão, pois a morte de uma ave poderá se dar com sua colisão com um único fio e, por isto, a maior proximidade entre os fios aumenta a probabilidade desta colisão.", "C)	Os ecologistas têm razão, uma vez que, ao encostar simultaneamente em dois fios, uma ave provavelmente morrerá eletrocutada.", "D)	Os ecologistas não têm razão, uma vez que, ao encostar simultaneamente em dois fios, uma ave nunca morrerá eletrocutada.", "E)	Os ecologistas não têm razão, pois sabe-se que o corpo de uma ave é um isolante elétrico, não permitindo a passagem de corrente elétrica.")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "C)	Os ecologistas têm razão, uma vez que, ao encostar simultaneamente em dois fios, uma ave provavelmente morrerá eletrocutada.") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

13. **(UFBA / Adaptada)** Numa usina hidrelétrica, a energia da queda-d'água é transformada em energia cinética de rotação numa turbina, em seguida em energia elétrica, num alternador, e finalmente é distribuída através de cabos de alta-tensão. Os princípios físicos envolvidos na produção e distribuição de energia permitem afirmar:

01\. A queda-d'água provoca uma perda de energia potencial gravitacional e um ganho de energia cinética de translação.

02\. A energia cinética de rotação da turbina é parcialmente transformada em energia elétrica, usando-se, para essa transformação, o fenômeno de indução eletromagnética.

04\. A resistência elétrica de um cabo de transmissão é diretamente proporcional ao seu comprimento e inversamente proporcional à sua área de secção transversal.

08\. Os transformadores situados na usina têm, para efeito da distribuição de energia em cabos de alta tensão, menor número de espiras na bobina primária do que na bobina secundária.

16\. Os transformadores convertem corrente alternada em corrente contínua e vice-versa. 32. A perda de energia elétrica, num cabo de transmissão, é diretamente proporcional à sua resistência e inversamente proporcional à corrente elétrica que o percorre.

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("32", "B)	12", "C)	15", "D)	34", "E)	63.")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "C)	15") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

14. **(FM-Itajubá-MG)** Um barco atravessa um rio seguindo a menor distância entre as margens, que são paralelas. Sabendo que a largura do rio é de 2,0 km, a travessia é feita em 15 min e a velocidade da correnteza é 6,0 km/h, podemos afirmar que o módulo da velocidade do barco em relação à água é:

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	2,0 km/h", "B)	6,0 km/h", "C)	8,0 km/h", "D)	10,0 km/h", "E)	14,0 km/h")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "D)	10,0 km/h") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)

```

15. **(Fuvest-SP)** As velocidades de crescimento vertical de duas plantas, A e B, de espécies diferentes, variaram, em função do tempo decorrido após o plantio de suas sementes, como mostra o gráfico. É possível afirmar que:

    ![](images/QUESTION15.png)

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	A atinge uma altura final maior do que B", "B)	B atinge uma altura final maior do que A", "C)	A e B atingem a mesma altura final", "D)	A e B atingem a mesma altura no instante t0", "E)	A e B mantêm altura constante entre os instantes t1 e t2")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "B)	B atinge uma altura final maior do que A") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

16. **(Fuvest-SP)** Num dia sem nuvens, ao meio-dia, a sombra projetada no chão por uma esfera de 1,0 cm de diâmetro é bem nítida se ela estiver a 10 cm do chão. Entretanto, se a esfera estiver a 200 cm do chão, sua sombra é muito pouco nítida. Pode-se afirmar que a principal causa do efeito observado é que:

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	o Sol é uma fonte extensa de luz", "B)	o índice de refração do ar depende da temperatura", "C)	a luz é um fenômeno ondulatório", "D)	a luz do Sol contém diferentes cores", "E)	a difusão da luz no ar “borra” a sombra")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "A)	o Sol é uma fonte extensa de luz") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

17. **(ENEM)** A figura mostra um eclipse solar no instante em que é fotografado em cinco diferentes pontos do planeta.

    ![](images/QUESTION17A.png) Três dessas fotografias estão reproduzidas abaixo.

    ![](images/QUESTION17B.png)

    ```{r echo=FALSE}
    library(shiny)

    question <- "Responda abaixo"
    alternatives <- c("A)	III, V e II", "B)	II, III e V ", "C)	II, IV e III", "D)	I, II e III", "E)	I, II e IV")

    # iNTERFACE DAS QUESTÕES

    ui <- fluidPage(
      titlePanel(
      ("     ")),
      mainPanel(
        h4(""),
        fluidRow(
          column(width = 12, h5(question)),
          column(width = 12, radioButtons("answer", "", choices = alternatives)),
          actionButton("submit", "Enviar resposta")
        )
      ),
      
      column(width = 12, 
        h4("Resultado:"),
        verbatimTextOutput("result")
    )
    )

    # Define o servidor do formulário
    server <- function(input, output) {
      # Mostra o resultado
      output$result <- renderPrint({
        if (input$submit == 0) {
          "Aguardando resposta…"
        } else if (is.null(input$answer)) {
          "Por favor, selecione uma resposta."
        } else if (input$answer == "A)	III, V e II") {
          "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
        } else {
          "Que pena, você errou a pergunta."
        }
      })
    }

    # Cria o aplicativo Shiny
    shinyApp(ui = ui, server = server)
    ```

    18.**(PUCC-SP)** Os raios de luz provenientes de uma estrela (E), ao atravessar a atmosfera, sofrem desvios, dando-nos a impressão de que a estrela está mais alta (E') do que realmente está (Figura 1). Também, por isso, pode-se observar a imagem do Sol (S') mesmo depois que ele (S) se pôs no horizonte ou antes de nascer.

    ![](images/QUESTION18.png)

    Esses fatos ocorrem, principalmente, devido à:

    ```{r echo=FALSE}
    library(shiny)

    question <- "Responda abaixo"
    alternatives <- c("A)	variação de índice de refração do ar com a altitude", "B)	variação de índice de refração do ar com a longitude", "C)	variação de índice de refração do ar com a latitude", "D)	dispersão da luz ao atravessar a atmosfera", "E)	forma esférica da Terra e à atração gravitacional sofrida pela Lua")

    # iNTERFACE DAS QUESTÕES

    ui <- fluidPage(
      titlePanel(
      ("     ")),
      mainPanel(
        h4(""),
        fluidRow(
          column(width = 12, h5(question)),
          column(width = 12, radioButtons("answer", "", choices = alternatives)),
          actionButton("submit", "Enviar resposta")
        )
      ),
      
      column(width = 12, 
        h4("Resultado:"),
        verbatimTextOutput("result")
    )
    )

    # Define o servidor do formulário
    server <- function(input, output) {
      # Mostra o resultado
      output$result <- renderPrint({
        if (input$submit == 0) {
          "Aguardando resposta…"
        } else if (is.null(input$answer)) {
          "Por favor, selecione uma resposta."
        } else if (input$answer == "A)	variação de índice de refração do ar com a altitude") {
          "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
        } else {
          "Que pena, você errou a pergunta."
        }
      })
    }

    # Cria o aplicativo Shiny
    shinyApp(ui = ui, server = server)
    ```

    19.**(UnB-DF / Adaptada)** O estabelecimento das ideias a respeito da gravitação universal é considerado uma das conquistas mais importantes no desenvolvimento das ciências em geral e, particularmente, da Física. A sua compreensão é fundamental para o entendimento dos movimentos da Lua, dos planetas, dos satélites e mesmo dos corpos próximos à superfície da Terra.

    Em relação a esse assunto, a alternativa incorreta:

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	Para que a Lua descreva o seu movimento orbital ao redor da Terra, é necessário que a resultante das forças que atuam sobre ela não seja nula.", "B)	Um satélite em órbita circular ao redor da Terra move-se perpendicularmente ao campo gravitacional terrestre.", "C)	A força gravitacional sobre um satélite sempre realiza trabalho, independentemente de sua órbita ser circular ou elíptica.", "D)	Um corpo, quando solto próximo à superfície terrestre, cai em direção a ela pelo mesmo motivo que a Lua descreve sua órbita em torno da Terra.", "E) N.D.A")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "C)	A força gravitacional sobre um satélite sempre realiza trabalho, independentemente de sua órbita ser circular ou elíptica.") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```

20. **(UFRJ/ADAPTADA)** Um escoteiro usa uma lupa para acender uma fogueira, concentrando os raios solares num único ponto a 20 cm da lupa. Utilizando a mesma lupa, o escoteiro observa os detalhes da asa de uma borboleta ampliada quatro vezes.![](images/QUESTION20.png)

    Calcule a que distância da asa da borboleta o escoteiro está posicionando a lupa:

```{r echo=FALSE}
library(shiny)

question <- "Responda abaixo"
alternatives <- c("A)	40 cm", "B)	55 cm", "C)	15 cm", "D)	23 cm", "E)	99 cm.")

# iNTERFACE DAS QUESTÕES

ui <- fluidPage(
  titlePanel(
  ("     ")),
  mainPanel(
    h4(""),
    fluidRow(
      column(width = 12, h5(question)),
      column(width = 12, radioButtons("answer", "", choices = alternatives)),
      actionButton("submit", "Enviar resposta")
    )
  ),
  
  column(width = 12, 
    h4("Resultado:"),
    verbatimTextOutput("result")
)
)

# Define o servidor do formulário
server <- function(input, output) {
  # Mostra o resultado
  output$result <- renderPrint({
    if (input$submit == 0) {
      "Aguardando resposta…"
    } else if (is.null(input$answer)) {
      "Por favor, selecione uma resposta."
    } else if (input$answer == "C)	15 cm") {
      "Parabéns! Você acertou a pergunta. Queima de combustíveis e energia"
    } else {
      "Que pena, você errou a pergunta."
    }
  })
}

# Cria o aplicativo Shiny
shinyApp(ui = ui, server = server)
```
